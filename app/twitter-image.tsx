/**
 * Twitter's card uses the same artwork as OpenGraph. Re-exported rather than
 * duplicated, so the two can never drift apart.
 */
export { alt, size, contentType, default } from "./opengraph-image";
